PCFG:
数括号，建立一个含有记录括号（分左括号和右括号）序号和紧接着的句法符号的哈希图，如果第一次遇到右括号，开始记录右括号、建立右括号的哈希图，并且把现有的扩括号的哈希图的最后记录的一个括号序号值与这个右括号的值相配对。
将这个规则截取出来，放进之前一开始即建立好的大的哈希图，这个大的哈希图是由规则的LHS作为key，而这个LHS对应的各种各样的RHS组成一个ArrayList作为它的value。
进而再次建立一个新的哈希图，这一次，以一整个规则的String作为这个哈希图的key，而把它相对应的概率作为这个哈希图的value。
最后一步，只需要把最后记录哥哥规则概率的哈希图打印到PrintStream里即可，完成。


PCKY:
