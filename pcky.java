import java.io.*;
import java.util.*;

public class pcky {
	public static void main(String[] args) throws IOException {
		String pcfg_path = args[0];
		String sent_path = args[1];
		PrintStream parses = new PrintStream(args[2]);

		BufferedReader sents = new BufferedReader(new FileReader(sent_path));

	}
}